export function createElizaLogger(oc) {
    const fmt = (...args) => args.map((a) => typeof a === "string" ? a : a instanceof Error ? `${a.name}: ${a.message}` : JSON.stringify(a)).join(" ");
    const dbg = oc.debug ?? oc.info;
    return {
        debug: (...a) => dbg(fmt(...a)),
        info: (...a) => oc.info(fmt(...a)),
        warn: (...a) => oc.warn(fmt(...a)),
        error: (...a) => oc.error(fmt(...a)),
        log: (...a) => oc.info(fmt(...a)),
        success: (...a) => oc.info(fmt(...a)),
        trace: (...a) => dbg(fmt(...a)),
    };
}
//# sourceMappingURL=logger-adapter.js.map